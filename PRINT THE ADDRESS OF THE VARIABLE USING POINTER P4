
#include <stdio.h>

int main() {
    int var = 10;
    int *ptr;

    ptr = &var; // ptr now holds the address of var

    printf("Address of var using pointer: %p\n", ptr);
    

    return 0;
}
Address of var using pointer: 0x7ffee78BC
